import 'package:flutter/material.dart';

class AppColors {
  // Base Theme Colors
  static const Color primary = Color(0xFF1E3A8A);        // Deep Indigo
  static const Color secondary = Color(0xFF3B82F6);      // Vivid Blue
  static const Color accent = Color(0xFFEF4444);         // Alert Red
  static const Color main = Color(0x4EB4F6);

  // Light Theme Colors
  static const Color background = Color(0xFFFFFFFF);      // Pure White
  static const Color cardLight = Color(0xFFF8F9FA);       // Very light grey card
  static const Color textPrimary = Color(0xFF1A1A1A);     // Almost black
  static const Color textSecondary = Color(0xFF555555);   // Dimmed text

  // Dark Theme Colors
  static const Color darkBackground = Color(0xFF121212);  // Pure dark background
  static const Color cardDark = Color(0xFF1E1E1E);         // Slightly elevated dark card
  static const Color textLight = Color(0xFFEAEAEA);        // Light for dark background
  static const Color textFaded = Color(0xFF9E9E9E);        // Muted for subtitles

  // Extras
  static const Color white = Colors.white;
  static const Color darkBlue = Color(0xFF0A1F44);
  static const Color lightBlue = Color(0xFF4A90E2);
}
